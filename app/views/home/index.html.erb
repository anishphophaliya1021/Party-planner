<% if logged_in? %>
<r style="font-family:Times New Roman;color:red; font-size:20px">
Welcome
<%= current_host.name.capitalize %>

<%else%>

<l style = "font-size:35px; font-family:Cambria;font-style:bold;color:#2E8B57">Welcome to the Party Planner</h1>
<br><br>

<% end %>
<% if !logged_in? %>
	<x style = "font-family:Times New Roman; font-size:25px;  ">
	<%= link_to 'Login', login_path %>
	/
	<%= link_to 'Signup', signup_path %>
	<x/>
<% end %>